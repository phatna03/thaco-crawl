# Railway config-as-code — https://docs.railway.com/config-as-code/reference
# Biến build Railpack (bắt buộc set trên Railway → Variables):
#   RAILPACK_PHP_VERSION=8.4
#   RAILPACK_PHP_EXTENSIONS=intl,zip

[build]
builder = "RAILPACK"
