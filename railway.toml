# Railway config-as-code — https://docs.railway.com/config-as-code/reference
# PHP 8.4: railpack.json (packages.php) — không phụ thuộc dashboard.
# Nếu thiếu ext-intl/zip khi build: Railway Variables → RAILPACK_PHP_EXTENSIONS=intl,zip

[build]
builder = "RAILPACK"
